import { Highliter2Directive } from './highliter2.directive';

describe('Highliter2Directive', () => {
  it('should create an instance', () => {
    const directive = new Highliter2Directive();
    expect(directive).toBeTruthy();
  });
});
