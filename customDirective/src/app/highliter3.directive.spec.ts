import { Highliter3Directive } from './highliter3.directive';

describe('Highliter3Directive', () => {
  it('should create an instance', () => {
    const directive = new Highliter3Directive();
    expect(directive).toBeTruthy();
  });
});
