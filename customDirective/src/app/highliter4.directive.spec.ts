import { Highliter4Directive } from './highliter4.directive';

describe('Highliter4Directive', () => {
  it('should create an instance', () => {
    const directive = new Highliter4Directive();
    expect(directive).toBeTruthy();
  });
});
